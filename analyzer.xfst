#include "options.h"

read lexc morph.cpp.lexc
define Lexicon;

source morph-phon.cpp.xfst

!
! Put all of them together. The order is important. 
!
clear stack
read regex     AFilter
           .o. Lexicon 
           .o. Abbr
#if (ANALYZER_APOSTROPHE_OPTIONAL == 1)
           .o. replaceAposOpt
#elif (ANALYZER_APOSTROPHE_REQUIRED == 1)
           .o. replaceApos
#endif
           .o. NumberSpace
           .o. NumericInsSilentP
           .o. NumericSeparator
           .o. CompNoun
           .o. DeleteSI
           .o. DeleteLAR
           .o. InsertN
           .o. DelS
           .o. PronounExceptions
           .o. InsertY
           .o. iDeleteY
           .o. NbeforePassive
           .o. PassiveLN
           .o. Causative
           .o. Optative3
           .o. SpaceBeforeMI
           .o. NegAorist
           .o. DeleteBuffer
           .o. DeYeException
           .o. YorException
           .o. VowelHarmony 
           .o. VowelEpenthesis
           .o. FSDevoicing
           .o. VoicingAssimilation
           .o. Redup
           .o. DeleteSilentP
           .o. KiKu
           .o. DeleteCircumflex
           .o. DeleteBoundary
#if (ANALYZER_CAPITALIZE == 1)
           .o. Capitalize
#endif
#if (ANALYZER_ALLCAPS == 1)
           .o. AllCaps 
#endif
           ;

save stack trmorph.fst
